<div class="navigation-agileits">
    <div class="container">
        <nav class="navbar navbar-default">
            <div class="navbar-header nav_2">
                <button type="button" class="navbar-toggle collapsed navbar-toggle1" data-toggle="collapse" data-target="#bs-megadropdown-tabs">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse" id="bs-megadropdown-tabs">
                <ul class="nav navbar-nav">
                    @* Manuel href yerine asp-controller ve asp-action kullandık *@
                    <li class="active"><a asp-controller="HomePage" asp-action="Index" class="act">Ana Sayfa</a></li>
                    <li><a asp-controller="HomePage" asp-action="ProductList">Ürünlerimiz</a></li>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <li><a asp-controller="Cart" asp-action="Index">Sepetim</a></li>
                        <li><a asp-controller="Order" asp-action="GetOrders">Siparişlerim</a></li>
                    }

                    <li><a asp-controller="HomePage" asp-action="AboutUs">Hakkımızda</a></li>
                    <li><a asp-controller="Contact" asp-action="Create">İletişim</a></li>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item" style="position:absolute;right:80px">
                            <a class="nav-link text-white" asp-controller="Customer" asp-action="Profile">@User.Identity.Name</a>
                        </li>
                        <li class="nav-item" style="position:absolute;right:0px">
                            <a class="nav-link text-white" asp-controller="Account" asp-action="Logout">Logout</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item" style="margin-left:547px">
                            <a class="nav-link text-white" asp-controller="Account" asp-action="Login">Giriş Yap</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" asp-controller="Account" asp-action="Register">Kayıt Ol</a>
                        </li>
                    }
                </ul>
            </div>
        </nav>
    </div>
</div>